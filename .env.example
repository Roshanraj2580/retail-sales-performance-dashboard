# Copy to .env and fill in. .env is gitignored — never commit real credentials.

# 'sqlite' (no setup) or 'postgres'
DB_ENGINE=sqlite

# SQLite
SQLITE_PATH=data/retail.db

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=retail
DB_USER=postgres
DB_PASSWORD=
